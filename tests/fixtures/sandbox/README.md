# Sandbox

**Delete this entire folder before merging.**

Output directory for `scripts/runSerializeDemo.js`. Run it to regenerate:

```bash
npm run build:library
node scripts/runSerializeDemo.js
```

`SERIALIZED-OUTPUT.json` is the result — the full JSON output of
`serializeDemos` run against the demos in `tests/fixtures/demos.js`.
